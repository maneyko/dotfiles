#!/usr/bin/env python3

import ipaddress
import json
import sys

def range_to_cidr(first_ip, last_ip):
    return ipaddress.summarize_address_range(ipaddress.IPv4Address(first_ip), ipaddress.IPv4Address(last_ip))

for cidr in range_to_cidr(sys.argv[1], sys.argv[2]):
    print(cidr)
