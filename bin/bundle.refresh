#!/bin/bash

set -e

source "argparse.sh"

arg_help "[Reload the Gemfile.lock from scratch.]"
parse_args

if [[ ! -f Gemfile ]]; then
  echo "'Gemfile' needs to be present in this directory to run this script." >&2
  exit 1
fi

rm -f Gemfile.lock
bundle install
