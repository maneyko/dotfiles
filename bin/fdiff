#!/bin/bash

sys_diff="$(which -a diff | tail -1)"

if [[ -f /usr/local/bin/diff && -n "$(command -v diff-so-fancy)" ]]; then
  /usr/local/bin/diff --color -u $@ | diff-so-fancy
else
  $sys_diff $@
fi
