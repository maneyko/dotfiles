#!/bin/bash

if test $MAC_OS
then
  sysbuild
  brew update
  brew update
  brew upgrade
else
  apt-get update
  apt-get dist-upgrade
fi

$HOME/.bin/pip-upgrade
$HOME/.bin/pip-upgrade -3
npm update -g
npm upgrade -g
test $MAC_OS && brew doctor
. ~/.bashrc > /dev/null
