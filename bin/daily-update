#!/bin/bash

sysbuild
brew update
brew update
brew upgrade
brew cask update
brew cask update
$HOME/.bin/pip-upgrade
$HOME/.bin/pip-upgrade -3
npm update -g
npm upgrade -g
vim +PluginUpdate +qall
brew doctor
. ~/.bashrc > /dev/null

