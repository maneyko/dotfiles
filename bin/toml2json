#!/usr/bin/env python3 -u

import sys
if sys.version_info < (3, 11):
    print("Python >= 3.11 must be used")
    sys.exit(1)

import json
import tomllib

f = open(sys.argv[1], "rb") if sys.stdin.isatty() else sys.stdin.buffer
print(json.dumps(tomllib.load(f)))
