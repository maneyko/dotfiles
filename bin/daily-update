#!/bin/bash

if test $MACOS
then
  sysbuild
  brew update
  brew update
  brew upgrade
else
  apt-get update
  apt-get dist-upgrade
fi

$HOME/.bin/pip-upgrade
$HOME/.bin/pip-upgrade -3
npm update -g
npm upgrade -g
test $MACOS && brew doctor
. ~/.bashrc > /dev/null
